// Frame header schema shared by C and Go for TCP stream framing.
proto frameheader;

enum PayloadType : uint8 {
    PAYLOAD_TYPE_UNKNOWN = 0;
    PAYLOAD_TYPE_DRONE = 1;
}

message FrameHeader {
    uint8 magic = 1;
    PayloadType payload_type = 2;
    uint16 payload_length = 3;
}
